class Scene{
  constructor(spheres, rectangles, camera, width=100, height=100){
    this.spheres = spheres;
    this.rectangles = rectangles;
    this.camera = camera;
    this.width = width;
    this.height = height
  }

}

