class Collision{
  constructor(timeToCollision, collisionLocation, normalAtCollision)
  {
    this.timeToCollision = timeToCollision;
    this.collisionLocation = collisionLocation;
    this.normalAtCollision = normalAtCollision
  }
}